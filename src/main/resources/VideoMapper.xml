<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.springmovie.mapper.VideoMapper">

    <!-- ResultMap 是可选的，如果字段名和列名一致可以省略 -->
    <!-- <resultMap id="VideoResultMap" type="com.example.springmovie.entity.Video">
        <result property="id" column="id" />
        <result property="type" column="type" />
    </resultMap> -->

    <!-- 查询视频类型 -->
    <select id="findVideoTypeById" resultType="string">
        SELECT type FROM videos WHERE id = #{id}
    </select>

    <!-- 其他映射语句可以继续添加 -->

</mapper>